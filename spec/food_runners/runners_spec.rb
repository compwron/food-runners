require 'spec_helper'

describe FoodRunners do
  it 'has a version number' do
    expect(FoodRunners::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
