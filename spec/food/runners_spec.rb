require 'spec_helper'

describe Food::Runners do
  it 'has a version number' do
    expect(Food::Runners::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
